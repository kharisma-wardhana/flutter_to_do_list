class Task {
  final String id;
  final String title;
  final String desc;
  final DateTime updatedAt;
  final DateTime createdAt;

  Task(this.id, this.title, this.desc, this.updatedAt, this.createdAt);
}
